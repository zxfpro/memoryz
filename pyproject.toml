[project]
name = "memoryz"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.115.12",
    "uvicorn>=0.34.2",
]

[tool.pytest.ini_options]
testpaths = [ "tests",]
pythonpath = [ "src",]
